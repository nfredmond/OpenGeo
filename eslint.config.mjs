import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Underscore-prefix is the idiomatic "intentionally unused" signal in TS.
  // Match the @typescript-eslint/no-unused-vars convention so mocks and
  // route-handler signatures that deliberately ignore args stay quiet.
  {
    rules: {
      "@typescript-eslint/no-unused-vars": [
        "warn",
        {
          argsIgnorePattern: "^_",
          varsIgnorePattern: "^_",
          caughtErrorsIgnorePattern: "^_",
          destructuredArrayIgnorePattern: "^_",
        },
      ],
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
    // Workflow DevKit generated route bundle (git-ignored, regenerated on build).
    "app/.well-known/workflow/**",
  ]),
]);

export default eslintConfig;
