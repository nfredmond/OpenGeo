# --- Node / Next.js ---
node_modules/
.pnpm-store/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# --- Env / secrets ---
.env
.env.*
!.env.example
!.env.test
*.key
*.pem
.r2-secrets.tmp

# Plaintext credential / provisioning notes — never commit.
private/
OpenGeo supabase info.md

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
logs/

# --- Test / coverage ---
coverage/
.nyc_output/
*.lcov

# --- Editor / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
*.swp
*.swo

# --- Vercel ---
.vercel/

# --- Supabase CLI ---
supabase/.branches/
supabase/.temp/

# --- Python (for ODM / AI pipelines) ---
.venv/
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/
.ruff_cache/

# --- Docker / data ---
data/
.docker/
docker/volumes/

# --- Large geo artifacts — never commit ---
*.tif
*.tiff
*.cog.tif
*.pmtiles
*.mbtiles
*.las
*.laz
*.copc.laz
*.shp
*.shx
*.dbf
*.prj
*.cpg
*.gpkg
*.gdb/
*.geoparquet
*.parquet
# Allow small fixture datasets explicitly placed under tests/fixtures/
!tests/fixtures/**/*.geojson
!tests/fixtures/**/*.json

# --- AI / model cache ---
.cache/
models/
checkpoints/

# --- Tool output ---
.turbo/
.sst/
.vercel

# --- MCP client session artifacts ---
# `.mcp.json` documents MCP servers available to local AI clients (Claude Code,
# Codex) against this project. Keep it untracked by default so each developer
# can point at their own MCP endpoints without leaking tokens; check it in
# explicitly if the team agrees on shared MCP config.
.mcp.json
# `.playwright-mcp/` holds page snapshots + console logs from Playwright-MCP
# browser sessions — ephemeral artifacts, never source.
.playwright-mcp/
